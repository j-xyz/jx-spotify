# Current Status

- Glow-baseline rebaseline landed (2026-04-13): `spotify_player` now keeps `content_shell_rect` full-width through `100x32`, `120x36`, `140x40`, and `180x48` so app chrome, content rails, SearchTui text/query rails, and the footer/help shelf land at x=2 like `jx-glow`; the body/footer split still exists, but the footer now shares the same content rail instead of the older bounded posture.
- Last verification: `cargo test --manifest-path spotify_player/Cargo.toml footer_chrome_uses_shared_content_rail_in_fixture_matrix`, `cargo test --manifest-path spotify_player/Cargo.toml app_chrome_alignment_matches_glow_baseline_shell_matrix`, `cargo test --manifest-path spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_glow_baseline`, `cargo test --manifest-path spotify_player/Cargo.toml content_rail_matches_glow_alignment_matrix`, and `cargo test --manifest-path spotify_player/Cargo.toml shell_fixture_matrix_matches_glow_baseline` all passed.
- Next safest step: do a quick PTY spot check at `100x32`, `120x36`, `140x40`, and `180x48` to confirm the footer/help strip now shares the glow rail while the body and chrome stay put.

- Phase 3 library/context live PTY review completed (2026-04-13): reran `spotify_player` in a real PTY at `120x36` and `140x40`, focused on library columns plus direct album/artist context opens, and confirmed that the shared `| ` rail now reads consistently across library, album context, and artist context without disturbing the Phase 0 shell/header rhythm or the Phase 2 footer/popup behavior.
- Observed behavior: popup-local `g` -> `Esc` still dismissed cleanly, the temporary `Cannot determine the current page's context` / `Loading...` transition still cleared without sticky render faults, and the wider multi-field artist-albums table spacing still read as an intentional local density choice rather than a broken gutter.
- Last verification: `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml` and `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml`, exercising library focus switching, direct album/artist context opens, and popup-local dismissal.
- Next slice: keep the wider `column_spacing(2)` multi-field table gutters local for now; if Phase 3 promotion work continues, compare the stabilized shared `| ` rail against the first cross-app target before reopening any table-gap normalization.

- Phase 3 row-rail consistency slice completed (2026-04-13): audited the active `jx-ui` shell/header/gutter contract across `jx-glow`, `jx-twig`, and `jx-spotify`, confirmed the sibling apps are still coherent against their fixture-backed references, and switched SearchTui onto the shared selected-row rail helper so its gutter spacing now matches the rest of `jx-spotify`'s row-grammar surfaces.
- Last verification: `cargo check --manifest-path spotify_player/Cargo.toml`, `cargo test --manifest-path spotify_player/Cargo.toml highlight_symbol_uses_shared_rail_with_trailing_gap`, `cargo test --manifest-path spotify_player/Cargo.toml gutter_selection_style_drops_background_fill`, `cargo test --manifest-path spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, `cargo test --manifest-path spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm`, and `cargo test --manifest-path spotify_player/Cargo.toml shortcut_family_popup_escape_only_dismisses_popup` all passed.
- Next slice: run a live PTY review of the library-columns plus direct album/artist context-opens slice at `120x36` and `140x40`; keep the remaining wider `column_spacing(2)` table gutters on multi-field tables unless that pass shows a concrete mismatch worth reopening.

- Phase 3 deeper authenticated non-SearchTui permutation validation completed (2026-04-11): reran full `spotify_player` PTY coverage at `100x32`, `120x36`, `140x40`, and `180x48`, exercising browse category drill-in (`g b` -> `Enter` -> `Enter` -> `Esc` -> `Esc`), library focus/window switching (`Tab`/`Shift-Tab`) with direct album/artist context opens from library columns, search-driven show context routing (`g s` -> `podcast` -> `Tab` to shows -> `Enter`), `g c` current-context routing, and popup-local `g` -> `Esc`.
- Observed behavior: context transitions briefly rendered `Cannot determine the current page's context` while data loaded, then resolved to the expected page content without sticky render faults or navigation regressions.
- Last verification: `cargo check --manifest-path spotify_player/Cargo.toml`, `cargo test --manifest-path spotify_player/Cargo.toml action_list_go_to_radio_pushes_radio_context`, `cargo test --manifest-path spotify_player/Cargo.toml radio_shortcut_family_hides_current_track_without_playback`, `cargo test --manifest-path spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `stty cols 100 rows 32; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 180 rows 48; cargo run --manifest-path spotify_player/Cargo.toml`, plus a targeted `120x36` confirmation run for search-to-show context (`g s` -> `podcast` -> `Tab` to `shows` -> `Enter`).
- Next slice: keep accent tuning deferred unless a concrete mismatch appears; if additional authenticated coverage is requested, prioritize episode-action `GoToShow` paths and deeper library folder/window permutations while preserving the Phase 0 shell matrix and Phase 2 popup/footer contract.
- Phase 3 authenticated non-SearchTui validation completed (2026-04-11): ran full `spotify_player` in a real PTY at `100x32`, `120x36`, `140x40`, and `180x48`, exercising `g l` (library), `g b` (browse), `g c` (current context), and popup-local `g` -> `Esc` -> `Q`; library, browse, and context-page transitions stayed stable with no pending-family footer regressions and no row-emphasis accent mismatch observed.
- Last verification: `stty cols 100 rows 32; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml`, `stty cols 180 rows 48; cargo run --manifest-path spotify_player/Cargo.toml`, `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml action_list_go_to_radio_pushes_radio_context`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml radio_shortcut_family_hides_current_track_without_playback`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open` all passed.
- Next slice: keep accent tuning deferred unless a concrete mismatch appears; if additional live coverage is needed, prioritize deeper authenticated non-SearchTui permutations (browse category drill-in, library focus/window switching, and artist/album/show context pages) while preserving the Phase 0 shell matrix and Phase 2 popup/footer contract.
- Phase 3 nested-family and non-search-path validation completed (2026-04-11): reran `search-tui` in a real PTY at `100x32`, `120x36`, `140x40`, and `180x48` with nested-family coverage (`g x` and `s l`), popup-local `Esc`, and `?` preview/full-help toggles; popup titles/footers and pending-family hints remained stable (including `g x: g` and `s l: a r`) with no row-emphasis accent mismatch observed.
- Last verification: `stty cols 100 rows 32; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 180 rows 48; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml radio_shortcut_family_hides_current_track_without_playback`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml action_list_go_to_radio_pushes_radio_context`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml preferred_external_glow_target_dir_`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm` all passed.
- Next slice: keep accent tuning deferred unless a concrete mismatch appears; if additional live validation is requested, prioritize authenticated non-SearchTui surfaces (`browse`, `library`, and context pages) while preserving the Phase 0 shell matrix and Phase 2 popup/footer contract.
- Phase 3 broader key-family live-terminal validation completed (2026-04-11): reran `search-tui` in a real PTY at `100x32`, `120x36`, `140x40`, and `180x48` and cycled `a`, `m`, `r`, `s`, `u`, `g`, popup-local `Esc`, plus `?` preview/full-help toggles before `Q`; popup titles/footers stayed aligned, pending-family hints remained stable (including `g: c t r y +11 esc cancel`), and no row-emphasis accent mismatch appeared.
- Last verification: `stty cols 100 rows 32; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 180 rows 48; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml gutter_selection_style_drops_background_fill`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_escape_only_dismisses_popup`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm` all passed.
- Next slice: keep accent tuning deferred unless a concrete mismatch appears, and if further live validation is needed, prioritize nested-family and non-search-page permutations while preserving the Phase 0 shell matrix and Phase 2 popup/footer contract.
- Phase 3 row-grammar live-terminal validation completed (2026-04-11): ran `search-tui` in a real PTY at `100x32`, `120x36`, `140x40`, and `180x48` using `g` -> `Esc` -> `Q`; selected-row gutter emphasis stayed readable, current-playing contrast stayed clear, and Phase 2 popup/footer behavior remained stable (`g: c t r y +11 esc cancel` while popup open).
- Last verification: `stty cols 100 rows 32; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 120 rows 36; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 140 rows 40; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `stty cols 180 rows 48; cargo run --manifest-path spotify_player/Cargo.toml search-tui`, `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml gutter_selection_style_drops_background_fill`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_escape_only_dismisses_popup`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm` all passed.
- Next slice: keep Phase 3 accent tuning deferred unless a concrete mismatch appears, and widen live validation coverage to additional key-family visual permutations while preserving the Phase 0 shell matrix and Phase 2 popup/footer contract.
- Phase 3 row-grammar convergence started (2026-04-11): row emphasis now uses a shared gutter-first selection treatment across selectable lists and tables (search, SearchTui, artist albums, context tracks, and context episodes), with selection normalization stripping background-fill/reverse so the left marker remains the primary cue.
- Theme/example alignment update (2026-04-11): `examples/theme.toml` no longer documents selection with a forced background fill, so documented defaults match the calmer gutter-marker direction.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml gutter_selection_style_drops_background_fill`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_escape_only_dismisses_popup`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm` all passed.
- Next slice: run a real PTY matrix spot-check (`100x32`, `120x36`, `140x40`, `180x48`) focused on row emphasis readability/current-playing contrast, then only tune accents if a concrete visual mismatch appears.
- Live-terminal matrix pass completed (2026-04-11): ran `jx-spotify` in a real PTY at `100x32`, `120x36`, `140x40`, and `180x48` (`g` to open family popup, `Esc` to dismiss, `Q` to exit); pending-family footer hints remained readable/aligned (`g: c t r y +11 esc cancel`) with no spacing regressions observed.
- Phase 2 command-shelf convergence continued (2026-04-11): while a shortcut-family popup is active, the right footer shelf now switches to contextual pending hints (`<family>:` plus child keys) with compact overflow (`+N`) and explicit `esc cancel`, replacing the static idle shelf until the family is resolved.
- Shortcut-family dismissal polish landed (2026-04-11): pressing `Esc` inside the shortcut-family popup now dismisses only that popup and no longer leaks into `PreviousPage` history navigation.
- Last verification: `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_key_hint_shows_pending_family_children_when_popup_is_open`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_escape_only_dismisses_popup`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shortcut_family_popup_`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm`, `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, plus live-terminal PTY runs at `100x32`, `120x36`, `140x40`, and `180x48` via `stty cols <w> rows <h>; cargo run --manifest-path spotify_player/Cargo.toml search-tui` all passed/behaved as expected.
- Next slice: close Phase 2 and begin Phase 3 row-grammar convergence, starting with low-risk row emphasis consistency passes while preserving the Phase 0 shell matrix posture.
- Phase 2 command-shelf convergence started (2026-04-11): the default right-aligned footer hints are now family-first (`a actions`, `g go`, `m mode`, `? help`) instead of the older search/help-only prompt, so the always-visible shelf now matches the suite family grammar before expanding to full help.
- Phase 1 header grammar slice landed (2026-04-10): the top chrome now renders `<badge> <context> <detail>` for the active page, with path/detail truncation applied before context truncation so the page noun stays stable in narrow shells.
- Phase 1 visible polish is now underway on row emphasis: selectable lists and tables now prefer a gutter-first highlight language with calmer row bodies, and SearchTui selected rows pick up a slightly stronger title accent without returning to full-row fill.
- Phase 1 `jx-ui` chrome geometry is now explicit in code: `content_shell_rect` still owns the default-shell matrix, and shared shell-text helpers now keep the `jx-spotify` badge, section headers, and SearchTui query shelf on the same inset rhythm instead of relying on scattered offsets.
- Phase 0 `jx-ui` fixture anchoring landed: `content_shell_rect` is now explicitly treated as the suite's default-shell reference, and the shell-width matrix for `100x32`, `120x36`, `140x40`, and `180x48` is locked into unit tests.
- Radio stability audit landed in commit `341dcb6` (`fix: stabilize radio shortcut flows`).
- Key invariant: do not hold `state.player` or `state.data` read locks across `GoToRadio` dispatch, because radio navigation persists local state (`last_radio_tracks_id`) before pushing the new context page.
- Follow-up audit across the broader `go to` family found the same deadlock pattern was radio-specific: other `go to` flows (`GoToAlbum`, `GoToArtist`, `GoToShow`, global page switches, search/list context opens) either operate on owned values first or only push UI/navigation state and let the page-change handler fetch context data afterward.
- Shortcut-family cleanup rule: hidden invalid keys inside a family popup should be consumed and cleared, while real prefixes and real exact commands still fall through to normal dispatch.
- The help-splitting direction has been reviewed and scoped.
- Current recommendation: keep the global command help page, then add smaller contextual surfaces starting with SearchTui.
- Radio-family context fix: the modal shortcut-family popup now filters its entries with the same playback/context rules as the shortcut help overlay, so invalid options like `r c` without current playback no longer appear as live submenu entries.
- Action-list routing fix: popup actions now use the owned-context dispatcher, so `GoToRadio` from action menus follows the same lock-safe path as the keyboard radio flows.
- SearchTui fallback fix: local fallback results now include saved tracks again, so `$song` queries can surface liked tracks even when remote search is empty.
- Help consistency slice: footer preview, context help, and README copy now lean on the shared `key to value` grammar, and duplicate `Enter` guidance was removed from context help.
- Right-meta alignment slice: track-bearing rows now render album and duration as separate slots inside the right column, with album right-aligned into the duration seam instead of a flat `album duration` string.
- Device-transfer routing investigation: `m d` only issues Spotify Web API playback transfer to a Spotify Connect `device_id`; AirPlay routing remains user-controlled on the Apple side and is not exposed through the Spotify device list or transfer API, so `jx-spotify` cannot guarantee that handing off to the iPhone will preserve an already-selected AirPlay speaker route.
- Default-directory hardening: outbound `g x g` handoffs now carry an explicit `target_dir` that prefers the active managed-project root when `jx-spotify` is launched inside the Maeve workspace, otherwise falls back to the Maeve workspace root instead of inheriting an arbitrary shell cwd like `$HOME`.
- Radio shortcut-family hardening: the popup now behaves as a self-contained modal, closes on unknown single-key input instead of leaking stale prefixes into the global key buffer, and will still dispatch a leaked full sequence like `g l` or `r s` if one reaches it. This is aimed at the recent “radio menu freeze” report.
- Implemented slices: the app now carries a persistent four-corner shortcut frame, `?` routes to the global help page, playback metadata is more compact, track-bearing surfaces now share a split-row visual language with primary title/artist content on the left and lighter metadata on the right, and the shortcut-family popup titles now cover `s` and `u`.
- Last verification: `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml preferred_external_glow_target_dir -- --nocapture` and `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after the default-directory handoff hardening.
- Last verification: `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml header_line_truncates_detail_before_context` and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml shell_fixture_matrix_matches_phase_zero_expectations` passed after wiring the Phase 1 header line helper into global app chrome.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml app_chrome_alignment_matches_phase_zero_shell_matrix`, and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml search_tui_text_and_query_shelf_follow_shell_rhythm` passed after the family-first footer hint update.
- Next slice: visual validation in the live TUI and any follow-up spacing or accent cleanup that falls out of that pass.
- Next slice: add contextual family-pending footer hints (`<family>:` plus children) and keep invalid-key dismissal behavior aligned with the shortcut-family popup contract before broader Phase 2 convergence.
- Recent related commit: `eb552eb` (`fix: move spotify badge to global header`).
- New restyle slice: compact now-playing footer, shorter playback window, album/radio table cleanup, global help family promotion, mouse-volume cleanup, and a footer mini-help preview on `?`.
- Mini-help preview update: the top-right family banner is gone, `?` now reveals a one-line footer preview first, and a second `?` falls through to full help.
- Footer layout update: the preview row and now-playing row now have separate footer lines, so they no longer fight for the same space.
- Worksuite handoff slice: `g x g` now maps to `GoExternalGlow`, emits a scoped JSON handoff envelope in the app cache, passes its path via `JX_GLOW_HANDOFF_FILE` for binary-compatibility, and exits spotify foreground on successful launch so terminal ownership is handed off cleanly; the shortcut-family popup supports nested families so multi-key paths like `g x g` are discoverable.
- Handoff teardown hardening: `GoExternalGlow` now queues a pending external launch in UI state and executes it only after raw mode is disabled and the alternate screen is left, preventing stacked-TUI/raw-mode race conditions during `jx-spotify` -> `jx-glow` transitions.
- Handoff process exchange hardening: on Unix, the post-teardown external launch now uses `exec` instead of `spawn+exit`, so `jx-spotify` is replaced in-place by `jx-glow` and foreground TTY ownership remains stable during handoff.
- Workspace ecosystem defaults: when `external_glow_command` is unset, `g x g` now prefers workspace-specific glow binaries (`/Users/jane/go/bin/jx-glow`, then `/Users/jane/jxyz/maeve/projects/jx-glow/jx-glow`) before generic PATH lookup for more deterministic cross-project handoff behavior.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after wiring workspace durable handoff defaults.
- Device preference tweak: `default_device` now wins over the currently active device when choosing a startup/auto-connect target, which should make home speaker handoff more predictable when headphones are also present.
- Radio resume slice: the last radio seed is now cached and loaded on startup so the current-playing page can reopen the last radio station after relaunch when Spotify no longer reports a live playback context.
- Split-row cleanup: search tracks, Search TUI results, queue rows, and context track lists now use the same full-width split layout, with the liked marker in a small leading slot, title/artist on the left, and lighter metadata aligned right.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after applying the shared split-row layout rule across track-bearing list views.
- Playback start hardening: when a `StartPlayback` request is issued without an already-active playback device, the client now resolves the preferred available device first and targets playback there instead of relying on a preexisting active device.
- New planning note: `docs/planning/sigil-entity-grammar-proposal-2026-04.md` captures a staged proposal to promote `@/$/!/#` from Search TUI sigils into a shared navigation, search, and in-list filtering grammar.
- Sigil grammar decision: the proposal is now accepted in principle for search and filtering, while sigil-driven `g` navigation bindings are intentionally deferred until the remapped symbols see real usage.
- Sigil remap: active sigils now align to `! album`, `@ artist`, and `$ song`; `#` and `%` are intentionally reserved until a clearer real-world meaning emerges.
- First sigil slice shipped: Search TUI now shows inline sigil hints, and popup search on track-bearing context lists now supports sigil-aware field filtering (`!` album, `@` artist, `$` song) instead of plain whole-row fuzzy matching only.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after adding sigil-aware track filtering and Search TUI sigil hints.
- New durable planning note: the workspace-root planning doc `docs/planning/suite-ui-convergence-plan-2026-04.md` now evaluates how far `jx-spotify` should converge with the newer `jx-glow` / `jx-twig` shell language and recommends a hybrid path: shared `jx-theme` startup palette, lighter chrome, bounded shell, and shell-width modal sheets, while keeping track tables compact and browse-page investment intentionally secondary. The document now normalizes its references as workspace-root-relative paths so a fresh agent can execute it without guessing the old in-project location.
- Security and operations constraint review: the planning pass confirmed that config/cache/log paths, cached user tokens, and `jx-glow` handoff envelopes are already created with private-file discipline; the main guardrail for the UI work is not to disturb raw-mode teardown, external handoff execution, or the current auth scope/write surface while refactoring rendering.
- Last verification: documentation-only review of `spotify_player/src/ui/mod.rs`, `spotify_player/src/ui/page.rs`, `spotify_player/src/ui/popup.rs`, `spotify_player/src/ui/playback.rs`, `spotify_player/src/config/mod.rs`, `spotify_player/src/config/theme.rs`, `spotify_player/src/client/mod.rs`, `spotify_player/src/auth.rs`, `spotify_player/src/token.rs`, and `spotify_player/src/event/mod.rs`, plus comparison against the active `jx-glow` / `jx-twig` planning notes and README guidance.
- Next slice: review the workspace-root planning doc `docs/planning/suite-ui-convergence-plan-2026-04.md` with Jane, decide whether `jx-theme` should become the literal default or a compatibility alias, then start with theme/chrome tokens and a shared shell helper before touching page-by-page layout or popup geometry.
- Hybrid-path Phase 1 started (2026-04-09): `jx-theme` is now built in as a first-class default theme in `spotify_player/src/config/theme.rs`, startup defaults now point to `jx-theme` in both `spotify_player/src/config/mod.rs` and `examples/app.toml`, and header badge rendering no longer uses ad hoc RGB literals (it now derives style from theme tokens via `theme.app_title()` in `spotify_player/src/ui/mod.rs`).
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after the Phase 1 theme/chrome foundation slice.
- Next slice: Phase 2 shell geometry helper in `spotify_player/src/ui/mod.rs` / `spotify_player/src/ui/utils.rs`, then one showcase page (`SearchTui`) under the bounded-shell framing before popup-sheet convergence.
- Hybrid-path Phase 2 started (2026-04-09): the UI now routes top chrome, body content, and popup space through a shared bounded-shell helper (`content_shell_rect`) so wide terminals no longer render edge-to-edge; shell width currently stays full on mid-size terminals and clamps to a centered max width with side breathing room on wide terminals.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` and `cargo test shell_ --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` both passed after shell geometry wiring and unit tests.
- Next slice: SearchTui-specific showcase tuning inside the new shell (layout balance/spacing), then shared popup-sheet backdrop work.
- SearchTui showcase slice landed (2026-04-09): `spotify_player/src/ui/page.rs` now presents Search TUI as a centered workspace within the bounded shell, with separate results and query panels instead of loose header lines; rows stay one line tall, but track and album results now render primary text and quieter secondary text separately for a calmer navigator feel.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` both passed after the SearchTui showcase pass.
- Next slice: move into `spotify_player/src/ui/popup.rs` for shared popup-sheet sizing and dimmed shell-backdrop behavior, then do a live TUI review around roughly `100x32`, `120x36`, `140x40`, and `180x48`.
- SearchTui flattening correction landed (2026-04-10): `spotify_player/src/ui/page.rs` now keeps Search TUI as a single primary surface inside the bounded shell, removes the extra vertical inset and separate results/query panel framing, preserves the calmer one-line row hierarchy, and recasts the query area as a shallow shelf strip under the results list.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` and `cargo test --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` both passed after the SearchTui flattening correction.
- Next slice: do live TUI validation of the flatter Search TUI around roughly `100x32`, `120x36`, `140x40`, and `180x48`, then move into `spotify_player/src/ui/popup.rs` for shared popup-sheet sizing and dimmed shell-backdrop behavior once the flatter direction is confirmed.
- SearchTui alignment correction landed (2026-04-10): the extra SearchTui-local width clamp was removed, so the search header and result rows now align directly under the main shell/header badge instead of sitting inside a second narrower column.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after removing the redundant SearchTui width clamp.
- SearchTui chrome/alignment refinement landed (2026-04-10): the shell now gives the `jx-spotify` badge a blank row above and below plus padded badge fill, SearchTui headers and query copy are offset onto the same text rhythm as the result rows, the selected-row marker shifts to a one-character-left `|`, and each result row now carries a leading glyph slot that dims by default, highlights on selection, and swaps to the liked-heart glyph for saved tracks.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after the SearchTui chrome/alignment refinement.
- SearchTui spacing polish landed (2026-04-10): header/query text shifted one column left to sit directly under the `jx-spotify` badge rhythm, and the selected-row marker now renders as `| ` so it keeps a visible space before the row glyph.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after the SearchTui spacing polish.
- SearchTui shape polish landed (2026-04-10): the filled `jx-spotify` badge now starts two columns into the shell to match the page text rhythm, and the selected-row marker now occupies the column immediately left of the row glyph column (`.|'selected row` in the alignment shorthand) instead of sitting flush against the shell edge.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after the SearchTui shape polish.
- SearchTui badge fill polish landed (2026-04-10): the `jx-spotify` badge now uses an inverted filled badge treatment derived from the theme token itself, so the accent color fills the badge and the app background color becomes the badge text instead of leaving the normal surface background behind.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after restoring the inverted badge fill.
- Last verification: `cargo test app_chrome_alignment_matches_phase_zero_shell_matrix --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test search_tui_text_and_query_shelf_follow_shell_rhythm --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test shell_fixture_matrix_matches_phase_zero_expectations --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, and `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` all passed after routing badge/header geometry through shared shell-text helpers.
- Last verification: `cargo test gutter_selection_style_drops_background_fill --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, `cargo test search_tui_text_and_query_shelf_follow_shell_rhythm --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml`, and `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` all passed after shifting the selection treatment toward gutter-first emphasis and calmer row bodies.
- Last verification: `cargo test shell_fixture_matrix_matches_phase_zero_expectations --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after adding the shared Phase 0 shell matrix expectations.
- Next slice: use the new Phase 0 fixture pack plus a live SearchTui pass at `100x32`, `120x36`, `140x40`, and `180x48` before moving into broader badge/header convergence or popup-sheet work.
- Auth startup hardening landed (2026-04-10): `spotify_player/src/client/mod.rs` now shares stale user-token-cache retry detection and applies it both during initial user-client token prompt and during session-start refresh, so HTTP 400 refresh failures clear `user_client_token.json` and force a one-time re-auth prompt instead of aborting launch immediately.
- Last verification: `cargo check --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` and `cargo test stale_user_client_cache_retry_requires_400_and_existing_cache --manifest-path /Users/jane/jxyz/maeve/projects/jx-spotify/spotify_player/Cargo.toml` passed after adding startup refresh recovery.
